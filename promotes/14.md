
1. 根据interface LocalFolder {
  id: string;
  name: string;
  path: string;
  songCount: number;
} 类型，在src-tauri/src/database 添加 local_folders Table
2. 在src-tauri/src/database/operations.rs 添加add_folder, get_folders (支持name 查询), delete_folders 函数并添加单元测试。
3. 并将这些api 添加到src-tauri/src/audio/commands.rs 中给前端调用。
4. 然后在src/hooks 添加这些函数调用和类型返回，并添加单元测试。最后src/components/Local/Local.tsx 调用这些函数。
5. 不要忘记运行cargo fmt -- --check，cargo clippy --all-features -- -D warnings -W clippy::pedantic -W clippy::nursery -W rust-2018-idioms，前端代码需要 code format 和 lint。

6. 修改src-tauri/src/audio/commands.rs add_folder function中的逻辑，验证path是否存在，不存在返回错误，然后递归检索文件内 mp3，flac，wav，ogg，m4a，aac 格式的文件，并将这些目标文件记述为song_count 存储到db。
    1. **Path Validation:** Implement `std::path::Path::exists()` check. Return `tauri::Error` on failure.
    2. **Recursive Scan:** Utilize `walkdir` crate for efficient directory traversal.
    3. **File Filtering:** Filter by `.mp3`, `.flac`, `.wav`, `.ogg`, `.m4a`, `.aac` extensions.
    4. **Database Storage:** Store `song_count` and file paths. Use `sqlx` or `diesel` for DB interaction.
    5. **Error Handling:** Ensure robust error propagation.

7. src/components/Library/Library.tsx 中 Recently Played，Made For You 提取为一个共用的component
8. 将 src-tauri/src/database/operations.rs 中的get_tracks 暴露给前端使用，然后在src/hooks 添加这些函数调用和类型返回，并添加单元测试。最后src/components/Library/Library.tsx 调用这些函数。在 Made For You 中使用这些数据进行渲染。
9. 为 LibrarySection 添加播放和暂停 事件，PlayerBar 状态和展示信息需要同步，进度条需要和rust 同步。
10. 创建播放列表，并关联tracks table，默认创建name 为 Recent, Favorites, Default，名称唯一。然后增加get_tracks_by_playlist，add_tracks_to_playlist, delete_tracks_from_playlist, delete_playlist 函数，并添加单元测试。并将这些函数暴露给前端使用，然后在src/hooks 添加这些函数调用和类型返回，并添加单元测试。
11. 在 MENU PLAYLISTS 侧边栏调用get_playlists 展示 Default, LIBRARY Recent Favorites 替换为从get_playlists获取到的Recent Favorites
12. 点击 Recent, Favorites, Default 侧边栏调用get_tracks_by_playlist 展示详情，共用LibrarySection 
